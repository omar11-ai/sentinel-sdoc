__pycache__/
*.pyc
.cache/
.env
# NEVER commit the organizers' private kit, ground truth, or secrets
data_v2/
uploads/
submissions/*.local.json
.env
.cache/
# note: data/ IS committed — it is the PUBLIC participant bundle
# (inbox/ attachments/ sample_submission.json) needed for cloud deployment.
# verified: contains no ground_truth.json and no generator scripts.
review_store.db
